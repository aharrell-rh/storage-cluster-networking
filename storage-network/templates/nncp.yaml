{{- range $i, $hostname := .Values.hostnames }}
{{- range $vlan := $.Values.vlans }}
{{- include "storage-network.nncp" (dict
    "hostname" $hostname
    "vlan" $vlan
    "index" $i
    "prefixLength" $.Values.prefixLength
) }}
---
{{- end }}
{{- end }}